{
    "layer": "top",
    "position": "bottom",
    "height": 30,
    "spacing": 4,
    
    // --------------//
    // MODULE GROUPS //
    // --------------//
    "modules-left": [
        "custom/power",
        "hyprland/workspaces",
        "mpris"
    ],
    "modules-center": [
        //"clock"
        "custom/clockblink"
        //"hyprland/window"
    ],
    "modules-right": [
        "pulseaudio",
        //"custom/sysstats",
        "cpu",
        "custom/gpu",
        "memory",
        //"temperature", DID NOT TRACK WELL WITH MY LAPTOP, AS IT BOOSTS TO 100C instead of achieving 100% use
        "battery",
        "network",
        "tray"
    ],
    
    // -------------//
    // MODULES-LEFT //
    // -------------//
   
    // (1/3) Combination of 'custom/power' & 'power-profiles-daemon'
    "custom/power": {
        "format" : "󰣇",
		"tooltip": false,
		"menu": "on-click",
		"menu-file": "$HOME/.config/waybar/custom_modules/power_menu.xml", // Menu file in resources folder
		"menu-actions": {
			"shutdown": "shutdown now",
			"reboot": "reboot",
			"suspend": "systemctl suspend",
			"hibernate": "systemctl hibernate",
			"lock": "hyprlock"
		}
    },
    
    // (2/3) Hyprland workspaces module
    "hyprland/workspaces": {
        "format": "{windows}",
        "format-window-separator": "",
        "workspace-taskbar": {
            "enable": true,
            "update-active-window": true,
            "format": "{icon}",
            "icon-size": 18,
            "on-click-window": "${SCRIPTS}/focus-window.sh {address} {button}"
        },
        "persistent-workspaces": {
            "*": [1, 2, 3, 4, 5, 6, 7, 8, 9]
        }
    },
    
    // (3/3) Media player (spotify)
    "mpris": {
        "player": "spotify",
        "format": "▶ {artist} - {title}",
        "format-paused": "⏸ {artist} - {title}",
        "format-stopped": "",
        "format-disconnected": "",
        "max-length": 60,
        "tooltip": false,

        "on-click": "playerctl -p spotify play-pause",
        "on-click-right": "playerctl -p spotify next",
        "on-click-middle": "playerctl -p spotify previous",
        "on-scroll-up": "playerctl -p spotify position 10+",
        "on-scroll-down": "playerctl -p spotify position 10-",
    },
    
    
    
    // ---------------//
    // MODULES-CENTER //
    // ---------------//
       
    // (1/1) Custom clock module. Weekday, day of month, hour:minute
    "custom/clockblink": {
        "exec": "~/.config/waybar/custom_modules/blinkclock.sh",
        "interval": 10,
        "return-type": "json",
        "format": "{text}",
        //"tooltip-format": "<big>{:%a %d-%m-%Y}</big>\n<tt><small>{calendar}</small></tt>"
    },
    
    // --------------//
    // MODULES-RIGHT //
    // --------------//
    
    // (1/8) Pulseaudio volume control with pavucontrol on click
    "pulseaudio": {
        "format": "{icon} {volume}%",
        "format-bluetooth": "{icon}  {volume}%",
        "format-bluetooth-muted": "  {volume}%",
        "format-muted": "{volume}%",
        "format-icons": {
            "headphone": "",
            "hands-free": "",
            "headset": "",
            "phone": "",
            "portable": "",
            "car": "",
            "default": ["", "", ""]
        },
        "on-click": "pavucontrol"
    },
    
    // (2/8) CPU usage
    "cpu": {
        "format": " {usage}%",
        "interval": 5
    },

    "custom/sysstats": {
        "exec": "~/.config/waybar/custom_modules/sysstats.sh",
        "interval": 5,
        "return-type": "json",
        "format": "{text}"
    },

    // (3/8) GPU usage (NVIDIA only, requires nvidia-smi)
    "custom/gpu": {
        "exec": "nvidia-smi --query-gpu=utilization.gpu --format=csv,noheader,nounits",
        "format": "󰍹 {}%",
        "interval": 5
    },
    
    // (4/8) Memory usage
    "memory": {
        "format": " {used:0.1f}GB/{total:0.1f}GB",
        "interval": 5
    },
    
    // (5/8) CPU Temperature (NOT USED, peeks at 100°C because of how AMD reports temp)
     "temperature": {
        "interval": 1,
        "hwmon-path": "/sys/class/hwmon/hwmon4/temp1_input",
        "critical-threshold": 105,
        "format-critical": " {temperatureC}°C",
        "format": "{temperatureC}°C"
    },
    
    // (6/8) Battery status
    "battery": {
        "states": {
            "good": 95,
            "warning": 30,
            "critical": 15
        },
        "format": "{icon} {capacity}%",
        "tooltip-format": "Health: {health} \n{timeTo}",
        "full-at": 95,
        "format-icons": ["", "", "", "", ""],
        "format-icons-charging": ["󰢜", "󰢜", "󰢜", "󰢜", "󰢜", "󰢜", "󰢜", "󰢜", "󰢜", "󰢜"]
    },
    
    // (7/8) Network status (WiFi & Ethernet)
    "network": {
        "format-wifi": " {ifname} {signalStrength}%",
        "format-ethernet": " {ifname}",
        "tooltip-format": " {ifname} via {gwaddr}",
        "format-linked": "󰈀 {ifname} (No IP) ",
        "format-disconnected": " Offline",
        "on-click": "kitty -e bluetui"
    },
    
    // (8/8) Tray
    "tray": {
        "spacing": 10
    },
}
